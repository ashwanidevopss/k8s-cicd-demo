apiVersion: v1
kind: Service
metadata:
  name: k8s-cicd-demo
  namespace: production
  labels:
    app: k8s-cicd-demo
spec:
  type: NodePort
  selector:
    app: k8s-cicd-demo
  ports:
  - port: 80
    targetPort: 3000
    nodePort: 30100
    protocol: TCP
    name: http

# Made with Bob
